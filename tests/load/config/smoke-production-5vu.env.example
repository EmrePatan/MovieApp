# Copy to gitignored .env.local — production first smoke only (5 VU).
# Do not commit.

LOAD_TEST_BASE_URL=https://your-production-api-host
LOAD_TEST_ENVIRONMENT=production
LOAD_TEST_TOKENS_FILE=./data/tokens.json
LOAD_TEST_CONTENT_DATASET=hot
LOAD_TEST_INCLUDE_EXTERNAL_RATINGS=false
LOAD_TEST_SEARCH_PROFILE=autocomplete-only
LOAD_TEST_BACKEND_SHA=deployed-sha-here
LOAD_TEST_DATA_DIR=./data
LOAD_TEST_THRESHOLDS_FILE=./config/thresholds.json
